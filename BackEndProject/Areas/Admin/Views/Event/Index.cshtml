@model IEnumerable<Event>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="content-wrapper col-12">
    <div class="row">
        <div class="col-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Basic Table</h4>
                    <div class="table-responsive">
                        <table class="table">
                            <a class="btn btn-primary" asp-action="Create">Create</a>
                            <thead>
                                <tr>
                                    <th>Profile</th>
                                    <th>Day</th>
                                    <th>Title</th>
                                    <th>Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (Event _event in Model)
                                {
                                    <tr>
                                        <td>
                                            <img style="width:60px; height:60px;" src="~/admin/images/faces/@_event.Image" />
                                        </td>
                                        <td style="max-width:200px">@_event.Day</td>
                                        <td style="max-width:300px">@_event.Title</td>
                                        <td>
                                            <a class="btn btn-success" asp-action="Read" asp-route-id="@_event.Id">Read</a>
                                            <a class="btn btn-warning" asp-action="Update" asp-route-id="@_event.Id">Update</a>
                                            <a class="btn btn-danger" asp-action="Delete" asp-route-id="@_event.Id">Delete</a>

                                        </td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>

