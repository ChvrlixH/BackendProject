@model IEnumerable<Teacher>
@{
    ViewData["Title"] = "Teacher Page";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="content-wrapper col-12">
    <div class="row">
        <div class="col-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Basic Table</h4>
                    <div class="table-responsive">
                        <table class="table">
                            <a class="btn btn-primary" asp-action="Create">Create</a>
                            <thead>
                                <tr>
                                    <th>Profile</th>
                                    <th>Fullname</th>
                                    <th>Profession</th>
                                    <th>Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (Teacher teachersItem in Model)
                                {
                                    <tr>
                                        <td>
                                            <img style="width:60px; height:60px;" src="~/admin/images/faces/@teachersItem.Image" />
                                        </td>
                                        <td style="max-width:200px">@teachersItem.Fullname</td>
                                        <td style="max-width:300px">@teachersItem.Profession</td>
                                        <td>
                                            <a class="btn btn-success" asp-action="Read" asp-route-id="@teachersItem.Id">Read</a>
                                            <a class="btn btn-warning" asp-action="Update" asp-route-id="@teachersItem.Id">Update</a>
                                            <a class="btn btn-danger" asp-action="Delete" asp-route-id="@teachersItem.Id">Delete</a>

                                        </td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>

